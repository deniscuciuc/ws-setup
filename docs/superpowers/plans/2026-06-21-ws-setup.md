# `ws-setup` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an Ansible-based provisioning repository that reproduces the current Ubuntu 26.04 developer workstation on a new laptop, plus a manual checklist and a Docker-based smoke test.

**Architecture:** A `bootstrap.sh` script installs Ansible and runs a playbook. The playbook applies small, single-purpose roles that install apt/snap packages, apply chezmoi-managed dotfiles, install developer tools, and render a post-provisioning checklist. Package lists are plain YAML under `group_vars/all/`.

**Tech Stack:** Ansible, chezmoi, bash, Docker (for testing), Ubuntu 26.04.

---

## File Structure

```
ws-setup/
├── bootstrap.sh
├── ansible.cfg
├── inventory/localhost.yml
├── playbook.yml
├── roles/
│   ├── bootstrap/
│   │   └── tasks/main.yml
│   ├── packages_apt/
│   │   └── tasks/main.yml
│   ├── packages_snap/
│   │   └── tasks/main.yml
│   ├── dotfiles/
│   │   └── tasks/main.yml
│   ├── devtools/
│   │   ├── tasks/main.yml
│   │   ├── tasks/nvm.yml
│   │   ├── tasks/pyenv.yml
│   │   └── tasks/dotnet.yml
│   └── manual_checklist/
│       ├── tasks/main.yml
│       └── templates/SETUP_CHECKLIST.md.j2
├── group_vars/all/
│   ├── apt_packages.yml
│   ├── snap_packages.yml
│   └── vars.yml
├── scripts/
│   └── validate.sh
└── tests/
    └── Dockerfile
```

---

### Task 1: Scaffold repository structure and Ansible configuration

**Files:**
- Create: `ansible.cfg`
- Create: `inventory/localhost.yml`
- Create: `playbook.yml`
- Create: directory tree under `roles/`
- Create: directory tree under `group_vars/all/`
- Create: `scripts/`, `tests/`

- [ ] **Step 1: Create `ansible.cfg`**

```ini
[defaults]
inventory = inventory/localhost.yml
roles_path = roles
retry_files_enabled = False
host_key_checking = False
interpreter_python = auto_silent
stdout_callback = yaml
nocows = 1

[privilege_escalation]
become_ask_pass = True
```

- [ ] **Step 2: Create `inventory/localhost.yml`**

```yaml
---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
```

- [ ] **Step 3: Create `playbook.yml`**

```yaml
---
- name: Provision Ubuntu 26.04 developer workstation
  hosts: localhost
  become: yes
  any_errors_fatal: false
  gather_facts: yes

  roles:
    - role: bootstrap
    - role: packages_apt
    - role: packages_snap
    - role: dotfiles
      become: false
    - role: devtools
      become: false
    - role: manual_checklist
      become: false
```

- [ ] **Step 4: Create empty role directories**

Run:
```bash
mkdir -p roles/{bootstrap,packages_apt,packages_snap,dotfiles,devtools,manual_checklist}/tasks
mkdir -p roles/devtools/tasks
mkdir -p roles/manual_checklist/templates
mkdir -p group_vars/all
mkdir -p scripts tests
```

- [ ] **Step 5: Commit**

```bash
git add ansible.cfg inventory/localhost.yml playbook.yml
git commit -m "chore: scaffold ansible project structure"
```

---

### Task 2: Generate and write apt package variables

**Files:**
- Create: `group_vars/all/apt_packages.yml`

- [ ] **Step 1: Generate manual apt list from current machine**

Run:
```bash
apt-mark showmanual | sort -u > /tmp/apt_manual.txt
```

- [ ] **Step 2: Write `group_vars/all/apt_packages.yml`**

Use the curated list below (derived from `/tmp/apt_manual.txt`). Keep hardware-specific and locale packages commented so the user can enable them per machine.

```yaml
---
# Core CLI and shell tools
apt_packages:
  - apt-transport-https
  - bat
  - btop
  - build-essential
  - ca-certificates
  - curl
  - fd-find
  - fzf
  - gh
  - git
  - gnupg
  - gnupg2
  - grep
  - gzip
  - htop
  - httpie
  - neovim
  - ncdu
  - openssh-server
  - pinentry-curses
  - python3-full
  - python3-pip
  - ripgrep
  - software-properties-common
  - speedtest-cli
  - tmux
  - tree
  - unzip
  - wget
  - wl-clipboard
  - yq
  - zip
  - zsh

  # Desktop environment and productivity
  - libreoffice-help-en-us
  - libreoffice-l10n-en-us
  - thunderbird-locale-en-us
  - ubuntu-desktop
  - ubuntu-desktop-minimal
  - ubuntu-restricted-addons

  # Development runtimes and containers
  - aspnetcore-runtime-10.0
  - containerd.io
  - docker-buildx-plugin
  - docker-ce
  - docker-ce-cli
  - docker-ce-rootless-extras
  - docker-compose-plugin
  - docker-model-plugin
  - dotnet-sdk-10.0

  # Uncomment on NVIDIA/CUDA machines only
  # - cuda-repo-ubuntu2604-13-3-local
  # - cuda-toolkit-13-3
  # - nvidia-cuda-toolkit
  # - nvidia-driver-595
```

- [ ] **Step 3: Validate YAML syntax**

Run:
```bash
python3 -c "import yaml; yaml.safe_load(open('group_vars/all/apt_packages.yml'))"
```
Expected: no output / exit 0.

- [ ] **Step 4: Commit**

```bash
git add group_vars/all/apt_packages.yml
git commit -m "feat: add curated apt package list"
```

---

### Task 3: Generate and write snap package variables

**Files:**
- Create: `group_vars/all/snap_packages.yml`

- [ ] **Step 1: Inspect current snaps**

Run:
```bash
snap list
```

- [ ] **Step 2: Write `group_vars/all/snap_packages.yml`**

Exclude base/runtime snaps (`core*`, `bare`, `mesa-*`, `gtk-common-themes`, `gnome-*`, `snapd*`, `cups`, `desktop-security-center`, `firmware-updater`, `gaming-graphics-core24`, `prompting-client`, `snap-store`).

```yaml
---
snap_packages:
  - { name: 0ad, classic: false }
  - { name: bitwarden, classic: false }
  - { name: code, classic: true }
  - { name: dbeaver-ce, classic: true }
  - { name: discord, classic: false }
  - { name: figma-linux, classic: false }
  - { name: firefox, classic: false }
  - { name: glab, classic: false }
  - { name: onlyoffice-desktopeditors, classic: false }
  - { name: organize-my-files, classic: false }
  - { name: redisinsight, classic: false }
  - { name: spotify, classic: false }
  - { name: steam, classic: false }
  - { name: telegram-desktop, classic: false }
  - { name: thunderbird, classic: false }
```

- [ ] **Step 3: Validate YAML syntax**

Run:
```bash
python3 -c "import yaml; yaml.safe_load(open('group_vars/all/snap_packages.yml'))"
```
Expected: no output / exit 0.

- [ ] **Step 4: Commit**

```bash
git add group_vars/all/snap_packages.yml
git commit -m "feat: add curated snap package list"
```

---

### Task 4: Create shared variables file

**Files:**
- Create: `group_vars/all/vars.yml`

- [ ] **Step 1: Write `group_vars/all/vars.yml`**

```yaml
---
# Repository and tooling constants
dotfiles_repo: "deniscuciuc"
chezmoi_install_url: "https://get.chezmoi.io"
chezmoi_bin_path: "{{ ansible_env.HOME }}/.local/bin"

# Installation flags (can be overridden per run)
install_nvm: true
install_pyenv: true
install_dotnet: true

# Paths
ws_setup_repo: "https://github.com/deniscuciuc/ws-setup.git"
ws_setup_dir: "{{ ansible_env.HOME }}/.local/share/ws-setup"
```

- [ ] **Step 2: Validate YAML syntax**

Run:
```bash
python3 -c "import yaml; yaml.safe_load(open('group_vars/all/vars.yml'))"
```
Expected: no output / exit 0.

- [ ] **Step 3: Commit**

```bash
git add group_vars/all/vars.yml
git commit -m "feat: add shared ansible variables"
```

---

### Task 5: Create the `bootstrap` role

**Files:**
- Create: `roles/bootstrap/tasks/main.yml`

- [ ] **Step 1: Write `roles/bootstrap/tasks/main.yml`**

```yaml
---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Ensure prerequisite packages are installed
  ansible.builtin.apt:
    name:
      - git
      - curl
      - software-properties-common
      - python3
      - python3-pip
      - python3-venv
    state: present

- name: Ensure ansible is installed via apt
  ansible.builtin.apt:
    name: ansible
    state: present
```

- [ ] **Step 2: Validate the role with ansible-playbook syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: `playbook: playbook.yml` with no syntax errors.

- [ ] **Step 3: Commit**

```bash
git add roles/bootstrap/tasks/main.yml
git commit -m "feat: add bootstrap role for ansible prerequisites"
```

---

### Task 6: Create the `packages_apt` role

**Files:**
- Create: `roles/packages_apt/tasks/main.yml`

- [ ] **Step 1: Write `roles/packages_apt/tasks/main.yml`**

```yaml
---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install curated apt packages
  ansible.builtin.apt:
    name: "{{ apt_packages }}"
    state: present
  register: apt_install_result
  retries: 3
  delay: 10
  until: apt_install_result is succeeded
```

- [ ] **Step 2: Run syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add roles/packages_apt/tasks/main.yml
git commit -m "feat: add apt package installation role"
```

---

### Task 7: Create the `packages_snap` role

**Files:**
- Create: `roles/packages_snap/tasks/main.yml`

- [ ] **Step 1: Write `roles/packages_snap/tasks/main.yml`**

```yaml
---
- name: Ensure snapd is installed and running
  ansible.builtin.apt:
    name: snapd
    state: present

- name: Wait for snapd seeding to complete
  ansible.builtin.command: snap wait system seed.loaded
  changed_when: false

- name: Install snaps
  ansible.builtin.command: >
    snap install {{ item.name }}
    {% if item.classic | default(false) %}--classic{% endif %}
    {% if item.channel | default('stable') != 'stable' %}--channel={{ item.channel }}{% endif %}
  args:
    creates: "/snap/{{ item.name }}/current"
  loop: "{{ snap_packages }}"
  register: snap_install_result
  retries: 3
  delay: 10
  until: snap_install_result is succeeded
```

- [ ] **Step 2: Run syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add roles/packages_snap/tasks/main.yml
git commit -m "feat: add snap package installation role"
```

---

### Task 8: Create the `dotfiles` role

**Files:**
- Create: `roles/dotfiles/tasks/main.yml`

- [ ] **Step 1: Write `roles/dotfiles/tasks/main.yml`**

```yaml
---
- name: Ensure ~/.local/bin exists
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.local/bin"
    state: directory
    mode: '0755'

- name: Check if chezmoi is installed
  ansible.builtin.command: which chezmoi
  register: chezmoi_which
  changed_when: false
  failed_when: false

- name: Download and install chezmoi
  ansible.builtin.shell: |
    set -o pipefail
    sh -c "$(curl -fsLS {{ chezmoi_install_url }})" -- -b {{ chezmoi_bin_path }}
  args:
    creates: "{{ chezmoi_bin_path }}/chezmoi"
  environment:
    PATH: "{{ ansible_env.PATH | default('') }}:{{ chezmoi_bin_path }}"
  when: chezmoi_which.rc != 0

- name: Ensure chezmoi is on PATH for this role
  ansible.builtin.set_fact:
    chezmoi_cmd: "{{ chezmoi_bin_path }}/chezmoi"
  when: chezmoi_which.rc != 0

- name: Use existing chezmoi binary
  ansible.builtin.set_fact:
    chezmoi_cmd: "chezmoi"
  when: chezmoi_which.rc == 0

- name: Initialize and apply dotfiles
  ansible.builtin.command: "{{ chezmoi_cmd }} init --apply {{ dotfiles_repo }}"
  args:
    creates: "{{ ansible_env.HOME }}/.zshrc"
  environment:
    PATH: "{{ ansible_env.PATH | default('') }}:{{ chezmoi_bin_path }}"
```

- [ ] **Step 2: Run syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add roles/dotfiles/tasks/main.yml
git commit -m "feat: add chezmoi dotfiles role"
```

---

### Task 9: Create the `devtools` role

**Files:**
- Create: `roles/devtools/tasks/main.yml`
- Create: `roles/devtools/tasks/nvm.yml`
- Create: `roles/devtools/tasks/pyenv.yml`
- Create: `roles/devtools/tasks/dotnet.yml`

- [ ] **Step 1: Write `roles/devtools/tasks/main.yml`**

```yaml
---
- name: Include nvm tasks
  ansible.builtin.include_tasks: nvm.yml
  when: install_nvm | default(true) | bool

- name: Include pyenv tasks
  ansible.builtin.include_tasks: pyenv.yml
  when: install_pyenv | default(true) | bool

- name: Include dotnet tasks
  ansible.builtin.include_tasks: dotnet.yml
  when: install_dotnet | default(true) | bool
```

- [ ] **Step 2: Write `roles/devtools/tasks/nvm.yml`**

```yaml
---
- name: Check if nvm is installed
  ansible.builtin.stat:
    path: "{{ ansible_env.HOME }}/.nvm/nvm.sh"
  register: nvm_script

- name: Install nvm
  ansible.builtin.shell: |
    set -o pipefail
    curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
  args:
    creates: "{{ ansible_env.HOME }}/.nvm/nvm.sh"
  when: not nvm_script.stat.exists

- name: Install latest LTS Node.js
  ansible.builtin.shell: |
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
    nvm install --lts
    nvm use --lts
    nvm alias default lts/*
  args:
    creates: "{{ ansible_env.HOME }}/.nvm/versions/node"
  when: not nvm_script.stat.exists
```

- [ ] **Step 3: Write `roles/devtools/tasks/pyenv.yml`**

```yaml
---
- name: Check if pyenv is installed
  ansible.builtin.command: which pyenv
  register: pyenv_which
  changed_when: false
  failed_when: false

- name: Install pyenv dependencies
  ansible.builtin.apt:
    name:
      - make
      - libssl-dev
      - zlib1g-dev
      - libbz2-dev
      - libreadline-dev
      - libsqlite3-dev
      - wget
      - curl
      - llvm
      - libncursesw5-dev
      - xz-utils
      - tk-dev
      - libxml2-dev
      - libxmlsec1-dev
      - libffi-dev
      - liblzma-dev
      - git
    state: present
  become: yes

- name: Install pyenv
  ansible.builtin.shell: |
    set -o pipefail
    curl https://pyenv.run | bash
  args:
    creates: "{{ ansible_env.HOME }}/.pyenv/bin/pyenv"
  when: pyenv_which.rc != 0
```

- [ ] **Step 4: Write `roles/devtools/tasks/dotnet.yml`**

```yaml
---
- name: Check if dotnet is installed
  ansible.builtin.command: which dotnet
  register: dotnet_which
  changed_when: false
  failed_when: false

- name: Install .NET SDK via Microsoft install script
  ansible.builtin.shell: |
    set -o pipefail
    curl -fsSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel 10.0 --install-dir "$HOME/.dotnet"
  args:
    creates: "{{ ansible_env.HOME }}/.dotnet/dotnet"
  when: dotnet_which.rc != 0
```

- [ ] **Step 5: Run syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: no errors.

- [ ] **Step 6: Commit**

```bash
git add roles/devtools/
git commit -m "feat: add devtools role for nvm, pyenv, and dotnet"
```

---

### Task 10: Create the manual checklist role and template

**Files:**
- Create: `roles/manual_checklist/tasks/main.yml`
- Create: `roles/manual_checklist/templates/SETUP_CHECKLIST.md.j2`

- [ ] **Step 1: Write `roles/manual_checklist/templates/SETUP_CHECKLIST.md.j2`**

```markdown
# New Laptop Setup Checklist

Generated after running `ws-setup` on {{ ansible_date_time.iso8601 }}.

## Before provisioning
- [ ] Back up files not stored in cloud storage.
- [ ] Create an Ubuntu 26.04 bootable USB.
- [ ] Ensure your password manager (Bitwarden) is accessible from another device.
- [ ] Note down any license keys for paid software.

## During provisioning
- [ ] Connect the new laptop to the internet.
- [ ] Open a terminal and run:
      ```bash
      curl -fsSL {{ ws_setup_repo }}/raw/main/bootstrap.sh | bash
      ```
      or clone `{{ ws_setup_repo }}` and run `./bootstrap.sh`.
- [ ] Enter your sudo password when prompted.
- [ ] Wait for the Ansible playbook to complete.

## After provisioning
- [ ] Import your SSH private key from your backup or password manager.
- [ ] Import your GPG key.
- [ ] Run `gh auth login` and authenticate with GitHub.
- [ ] Open Firefox/Chrome and sign in to sync bookmarks, passwords, and extensions.
- [ ] Sign into the Bitwarden browser extension.
- [ ] Configure Syncthing and pair it with your other devices.
- [ ] Sign into communication apps: Discord, Telegram, Thunderbird.
- [ ] Sign into productivity apps: Spotify, Figma, OnlyOffice.
- [ ] Install any paid/proprietary software not covered by apt/snap.
- [ ] Verify zsh is the default shell: `chsh -s $(which zsh)`.
- [ ] Run `chezmoi diff` to confirm dotfiles are applied.
- [ ] Reboot the machine.

## Optional / per machine
- [ ] Uncomment NVIDIA/CUDA packages in `group_vars/all/apt_packages.yml` and re-run the playbook if the new laptop has an NVIDIA GPU.
- [ ] Adjust GNOME settings, themes, keyboard shortcuts, and touchpad preferences manually.
```

- [ ] **Step 2: Write `roles/manual_checklist/tasks/main.yml`**

```yaml
---
- name: Ensure Desktop directory exists
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/Desktop"
    state: directory
    mode: '0755'

- name: Render post-provisioning checklist
  ansible.builtin.template:
    src: SETUP_CHECKLIST.md.j2
    dest: "{{ ansible_env.HOME }}/Desktop/SETUP_CHECKLIST.md"
    mode: '0644'

- name: Display completion message
  ansible.builtin.debug:
    msg: |
      Provisioning complete.
      Please open ~/Desktop/SETUP_CHECKLIST.md and complete the manual steps.
```

- [ ] **Step 3: Run syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add roles/manual_checklist/
git commit -m "feat: add manual checklist role and template"
```

---

### Task 11: Create the bootstrap script

**Files:**
- Create: `bootstrap.sh`

- [ ] **Step 1: Write `bootstrap.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_URL="https://github.com/deniscuciuc/ws-setup.git"
TARGET_DIR="${HOME}/.local/share/ws-setup"

ensure_dependency() {
  local cmd="$1"
  local pkg="${2:-$1}"
  if ! command -v "$cmd" >/dev/null 2>&1; then
    echo "Installing $pkg..."
    sudo apt-get update
    sudo apt-get install -y "$pkg"
  fi
}

echo "==> Bootstrapping workstation setup"
ensure_dependency git git
ensure_dependency curl curl
ensure_dependency python3 python3
ensure_dependency ansible ansible

if [ -d "$TARGET_DIR/.git" ]; then
  echo "==> Updating existing ws-setup repo"
  git -C "$TARGET_DIR" pull --ff-only
else
  echo "==> Cloning ws-setup repo"
  mkdir -p "$(dirname "$TARGET_DIR")"
  git clone "$REPO_URL" "$TARGET_DIR"
fi

cd "$TARGET_DIR"
echo "==> Running Ansible playbook"
ansible-playbook -i inventory/localhost.yml playbook.yml

echo "==> Done"
```

- [ ] **Step 2: Make it executable**

Run:
```bash
chmod +x bootstrap.sh
```

- [ ] **Step 3: Dry-run the bootstrap script with bash syntax check**

Run:
```bash
bash -n bootstrap.sh
```
Expected: no output / exit 0.

- [ ] **Step 4: Commit**

```bash
git add bootstrap.sh
git commit -m "feat: add bootstrap script"
```

---

### Task 12: Create the validation script

**Files:**
- Create: `scripts/validate.sh`

- [ ] **Step 1: Write `scripts/validate.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

ERRORS=0

check_command() {
  if command -v "$1" >/dev/null 2>&1; then
    echo "[OK] $1 found"
  else
    echo "[FAIL] $1 not found"
    ERRORS=$((ERRORS + 1))
  fi
}

check_package() {
  if dpkg -l "$1" >/dev/null 2>&1; then
    echo "[OK] apt package $1 installed"
  else
    echo "[FAIL] apt package $1 not installed"
    ERRORS=$((ERRORS + 1))
  fi
}

check_snap() {
  if snap list "$1" >/dev/null 2>&1; then
    echo "[OK] snap $1 installed"
  else
    echo "[FAIL] snap $1 not installed"
    ERRORS=$((ERRORS + 1))
  fi
}

check_command zsh
check_command git
check_command chezmoi
check_command nvim
check_command batcat
check_command fdfind
check_command rg
check_command fzf

check_package zsh
check_package git
check_package neovim
check_package bat

check_snap code
check_snap firefox

if chezmoi diff >/dev/null 2>&1; then
  echo "[OK] chezmoi diff succeeded"
else
  echo "[FAIL] chezmoi diff failed"
  ERRORS=$((ERRORS + 1))
fi

if [ "$ERRORS" -eq 0 ]; then
  echo "All validation checks passed."
  exit 0
else
  echo "$ERRORS validation check(s) failed."
  exit 1
fi
```

- [ ] **Step 2: Make it executable**

Run:
```bash
chmod +x scripts/validate.sh
```

- [ ] **Step 3: Syntax check**

Run:
```bash
bash -n scripts/validate.sh
```
Expected: no output / exit 0.

- [ ] **Step 4: Commit**

```bash
git add scripts/validate.sh
git commit -m "feat: add provisioning validation script"
```

---

### Task 13: Smoke test on the current machine

**Files:**
- Modify: as needed based on failures

- [ ] **Step 1: Run ansible-playbook syntax check**

Run:
```bash
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml
```
Expected: no syntax errors.

- [ ] **Step 2: Run ansible-playbook in check mode**

Run:
```bash
ansible-playbook -i inventory/localhost.yml playbook.yml --check --diff
```
Expected: playbook runs; some tasks may show changes for packages not yet installed or already installed. No fatal syntax/variable errors.

- [ ] **Step 3: Run the full playbook**

Run:
```bash
ansible-playbook -i inventory/localhost.yml playbook.yml
```
Expected: playbook completes. Note any failures in snap/devtools roles.

- [ ] **Step 4: Run the playbook again to verify idempotency**

Run:
```bash
ansible-playbook -i inventory/localhost.yml playbook.yml
```
Expected: `changed=0` or very few changes for non-idempotent tasks (e.g., snap `command` module). No failures.

- [ ] **Step 5: Run validation script**

Run:
```bash
./scripts/validate.sh
```
Expected: all checks pass.

- [ ] **Step 6: Commit any fixes from the smoke test**

```bash
git add -A
git commit -m "fix: smoke test adjustments"
```

---

### Task 14: Docker-based VM test

**Files:**
- Create: `tests/Dockerfile`
- Create: `tests/test.sh`

- [ ] **Step 1: Write `tests/Dockerfile`**

```dockerfile
FROM ubuntu:26.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
 && apt-get install -y sudo curl git python3 python3-pip ansible \
 && rm -rf /var/lib/apt/lists/*

# Create a test user with passwordless sudo to mimic laptop usage
RUN useradd -m -s /bin/bash tester \
 && echo "tester ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/tester

USER tester
WORKDIR /home/tester

COPY --chown=tester:tester . /home/tester/ws-setup/
WORKDIR /home/tester/ws-setup

CMD ["./tests/test.sh"]
```

- [ ] **Step 2: Write `tests/test.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

# The Dockerfile already installs ansible, but bootstrap.sh should also handle a clean system.
# Here we run the playbook directly to test the ansible logic.
ansible-playbook -i inventory/localhost.yml playbook.yml

# Validate
./scripts/validate.sh
```

- [ ] **Step 3: Make `tests/test.sh` executable**

Run:
```bash
chmod +x tests/test.sh
```

- [ ] **Step 4: Build and run the Docker test**

Run:
```bash
docker build -t ws-setup-test -f tests/Dockerfile .
docker run --rm ws-setup-test
```
Expected: image builds and container exits 0. Note that snaps cannot be tested inside Docker; the snap role will likely report skipped/failed snaps, which is acceptable for the Docker test.

- [ ] **Step 5: Commit**

```bash
git add tests/
git commit -m "test: add docker-based provisioning test"
```

---

### Task 15: Add README and final polish

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write `README.md`**

```markdown
# ws-setup

Ansible playbook to reproduce my Ubuntu 26.04 developer workstation on a new laptop.

## Quick start on a fresh machine

```bash
curl -fsSL https://github.com/deniscuciuc/ws-setup/raw/main/bootstrap.sh | bash
```

## Manual run

```bash
git clone https://github.com/deniscuciuc/ws-setup.git ~/.local/share/ws-setup
cd ~/.local/share/ws-setup
./bootstrap.sh
```

## What it does

1. Installs Ansible and prerequisites.
2. Installs curated apt and snap packages.
3. Installs chezmoi and applies `deniscuciuc/dotfiles`.
4. Installs developer tools (nvm, pyenv, .NET SDK).
5. Writes `~/Desktop/SETUP_CHECKLIST.md` with manual post-install steps.

## Testing

```bash
# Syntax check
ansible-playbook --syntax-check -i inventory/localhost.yml playbook.yml

# Dry run
ansible-playbook -i inventory/localhost.yml playbook.yml --check --diff

# Docker smoke test
docker build -t ws-setup-test -f tests/Dockerfile .
docker run --rm ws-setup-test
```

## Customization

- Edit `group_vars/all/apt_packages.yml` to change apt packages.
- Edit `group_vars/all/snap_packages.yml` to change snaps.
- Edit `group_vars/all/vars.yml` to toggle devtools or change versions.
- Per-machine overrides can go in `host_vars/<hostname>.yml`.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README with usage and testing instructions"
```

---

## Self-Review

After completing all tasks, verify:

1. **Spec coverage:** Every section of the design spec has at least one implementing task.
2. **No placeholders:** No `TBD`, `TODO`, or vague steps remain.
3. **Idempotency:** The full playbook runs twice on the current machine without unexpected changes.
4. **Docker test:** `docker run --rm ws-setup-test` exits 0.
5. **Secrets:** No SSH keys, GPG keys, passwords, or tokens are committed.
