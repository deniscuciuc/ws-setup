#!/usr/bin/env bash
# Pinned versions matching the reference Ubuntu 26.04 workstation.
# Update these values when the reference machine changes.

export NVM_INSTALL_VERSION="v0.40.4"            # nvm release tag, must include leading 'v'
export NODE_VERSION="v26.3.0"                   # Node.js version, must include leading 'v'
export PNPM_VERSION="11.5.2"                    # pnpm version, no leading 'v'
export COREPACK_VERSION="0.35.0"                # corepack npm package, no leading 'v'

export CLAUDE_CODE_VERSION="2.1.170"            # npm package version, no leading 'v'
export CODEX_VERSION="0.137.0"                  # npm package version, no leading 'v'
export KIMI_CODE_VERSION="0.18.0"               # npm package version, no leading 'v'

export GH_COPILOT_EXTENSION_VERSION="v1.2.0"    # gh extension release tag, must include leading 'v'
export GITHUB_COPILOT_CLI_VERSION="0.1.36"      # npm package version, no leading 'v'

export STARSHIP_VERSION="1.25.1"                # starship release version, no leading 'v'
export KITTY_VERSION="0.47.2"                   # kitty release version, no leading 'v'
